[package]
name = "mm2_net_config"
version = "0.1.0"
edition = "2021"
description = "Compile-time network configuration keyed by netid"

[dependencies]
lazy_static = "1"
hex = "0.4"
num-rational = { version = "0.4", features = ["num-bigint"] }
