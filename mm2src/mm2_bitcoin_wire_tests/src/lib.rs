//! KDF-original empty crate root. The wire-equivalence assertions live in
//! the integration test under `tests/`. This module exists only so the
//! crate compiles as a library and contributes a recognisable name to the
//! workspace.
