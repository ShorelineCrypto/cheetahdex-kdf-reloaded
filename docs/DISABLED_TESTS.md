# Disabled / Ignored Tests

Inventory and remediation status of tests currently marked `#[ignore]` in the
workspace: why each is disabled, the effort to re-enable, and what coverage is
lost while it stays off.

> **TODO:** populate this inventory. A full analysis of the current `#[ignore]`d
> tests (grouped by root cause — genuine code failures, dead external endpoints,
> live-infra integration tests, docker swap tests, hardware, and delete
> candidates) has been drafted but is not yet written up here.
